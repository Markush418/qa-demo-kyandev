OPENAI_API_KEY=sk-...              # Embeddings (text-embedding-3-small) + Chat (gpt-4o-mini)
DATABASE_URL=./data/docqa.db       # Path SQLite
MAX_FILE_SIZE_MB=20                # Límite de upload
MAX_PAGES=200                      # Límite de páginas
CLEANUP_HOURS=24                   # TTL de documentos
PORT=3000
# Opcional: sobrescribir modelos
# OPENAI_EMBED_MODEL=text-embedding-3-small
# OPENAI_CHAT_MODEL=gpt-4o-mini
